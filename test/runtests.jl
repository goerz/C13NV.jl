using TestItemRunner

@run_package_tests

# Tests can be run by via `include("test/runtests.jl")` in the development
# REPL. Individual tests files (`test/test_*.jl`) can also be included in the
# REPL to run only the tests in that file.
